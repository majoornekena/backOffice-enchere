import React, { Component } from "react";

export default class Place extends Component {
    render() {
        return (
             <React.Fragment>
                
             </React.Fragment>
        );
    }
}